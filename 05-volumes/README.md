# Volumes in Docker

This section is the answer to this question: How do I persist data in Docker containers?  
So far, we have run and removed containers multiple times, which means that any data created inside the container is lost when the container is removed. To persist data, we need to use volumes.