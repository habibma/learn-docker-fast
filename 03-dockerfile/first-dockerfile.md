## Container management
Assume you have a container running. You can stop it with the following command:

```bash
docker stop <container_id>
```

but what if you want to start it again? You can use the following command:

```bash
docker start <container_id>
```
